import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App'

const root = ReactDOM.createRoot(document.getElementById("merch"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
